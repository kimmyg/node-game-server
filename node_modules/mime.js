var mimeTypes = {
	'css': 'text/css',
	'html': 'text/html',
	'js': 'application/javascript'
};

var extensionPattern = /\.([^\.]+)$/

exports.typeForPath = function( path ) {
	var match = path.match( extensionPattern );

	if( match ) {
		return mimeTypes[ match[1] ];
	}
}
		
